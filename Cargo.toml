[workspace]
resolver = "2"
members = [
    "contracts/gapas",
    "contracts/gapas_farm",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1
strip = true
